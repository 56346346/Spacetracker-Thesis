<Window x:Class="SpaceTracker.PullDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pull Changes" Height="400" Width="600">
    <DockPanel Margin="10">
        <DataGrid x:Name="ChangesGrid" DockPanel.Dock="Top" AutoGenerateColumns="False" SelectionMode="Extended">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Element ID" Binding="{Binding ElementId}" Width="*" />
                <DataGridTextColumn Header="Typ" Binding="{Binding ChangeType}" Width="*" />
                <DataGridTextColumn Header="Zeit" Binding="{Binding Timestamp}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="ApplyButton" Content="Apply Selected" Width="100" Margin="0,0,5,0" Click="ApplyButton_Click"/>
            <Button x:Name="IgnoreButton" Content="Alle ignorieren" Width="100" Click="IgnoreButton_Click"/>
        </StackPanel>
    </DockPanel>
</Window>